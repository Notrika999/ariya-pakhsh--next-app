# Server-only: backend base URL (Swagger API)
# Legacy: BACKEND_ORIGIN=https://aryapakhsh.shop
BACKEND_ORIGIN=https://aryapakhsh.shop/swagger-api

# Public site URL for canonical, sitemap, Open Graph
NEXT_PUBLIC_SITE_URL=https://aryapakhsh.shop/swagger-api

# Legacy — if set to a relative path like /api/v1, BACKEND_ORIGIN is used instead.
# API_URL=https://aryapakhsh.shop
